changequote([,])dnl
define([Fucking],[Zogging])dnl
define([a],[b])dnl
define([bzzb],[TRUE])dnl
define([TRUE],[T])dnl
perl -e 'print "$x@<:@1@:>@\n";'
This is a bzzb text. Fucking amazing...
 if (a==b) then c:=3;
 if bzzb then c:=3;
# if bzzb then c:=3;
dnl if bzzb then c:=3;
 if ([b==b]) then c:=3;
ifdef(a,[yes],[no])
ifdef([a],[yes],[no])
ifdef([z],[yes],[no])
esyscmd(git describe | tr -d '\n')
A=esyscmd(git describe | tr -d '\n')
Last Line
